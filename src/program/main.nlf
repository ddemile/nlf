import { get } from "core:http"

let test = input("Press Enter to start fetching data...");

print("You entered:", test)

confirm("Are you gay?");

while true {
    let response = get("https://fakerapi.it/api/v2/addresses?_quantity=1")
    print(response)
}