let fs = binding("fs")

export fn read(path: string): string {
    return fs.read(path)
}

export fn write(path: string) {
    fs.write(path)
}

export fn rm(path: string) {
    fs.rm(path)
}

export fn rmdir(path: string) {
    fs.rmdir(path)
}