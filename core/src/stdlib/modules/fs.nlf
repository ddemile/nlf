let fs = binding("http")

export fn read(path) {
    return fs.read(path)
}

export fn write(path) {
    return fs.write(path)
}

export fn rm(path) {
    return fs.rm(path)
}

export fn rmdir(path) {
    return fs.rmdir(path)
}