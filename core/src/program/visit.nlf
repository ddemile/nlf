import { sum } from "./visit_lib.nlf"

let a = "string"
let b = true
let c: number = 454.5

fn function(my_str: string, e) {
    let nested = true

    my_str
}

let value = 4

class Paris {
    fn country(self): string {
        return "France"
    }
}

let city = Paris()

print(function, Paris, value, city.country())

print(sum([1, 2, 3, 4, 5]))