import { sum } from "./visit_lib.nlf"

let a = "string"
let b = true
let c: number = 454.5

fn function(my_str: string, e): string {
    let nested = true

    my_str
}

let add = (a: number, b: number): number => {
    return a + b
}

let value = add(8, 2)

let func = function("a", "b")

class Paris {
    Paris(): number {

    }

    fn country(self): string {
        return "France"
    }
}

let city = Paris()

let country = city.country()
let should_be_unknown = city.Paris()

print(function, Paris, value, country)

let result = sum([1, 2, 3, 4, 5])

print(result)