import {greet, get_key, set_key, increment_key} from "./mod.nlf"

let mark = "!"

let key = "value"

export fn concat(a: string, b: string) {
    return a + " " + b + mark
}

export fn mutate_key(obj, key, value) {
    obj[key] = value
}