export fn sum(arr): number {
    let result = 0
    for i in ..arr.len() {
        result += arr[i]
    }
    return result
}

export class Madrid {
    Madrid() {

    }

    fn country(self): string {
        return "Spain"
    }
}